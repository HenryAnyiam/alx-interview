import minOperations from "./0-minoperations.js";

let n;

n = 4;
console.log(`Min # of operations to reach ${n} char: ${minOperations(n)}`);

n = 12;
console.log(`Min # of operations to reach ${n} char: ${minOperations(n)}`);

n = 50;
console.log(`Min # of operations to reach ${n} char: ${minOperations(n)}`);